interface:
  display_name: "Prosa Search Export"
  short_description: "Search, sessions, analytics, and export guidance"
  default_prompt: "Use $prosa-search-export when changing prosa search, session listing, analytics, Parquet/DuckDB, or export behavior."
